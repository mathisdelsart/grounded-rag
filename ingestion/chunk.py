"""Adaptive chunking.

Slides: roughly one slide per chunk. Prose: ~500 tokens with overlap.
Each chunk carries {course, chapter, page} metadata used for citations.
"""

# TODO: implement chunk_pages(pages, doc_type) -> list[Chunk]
